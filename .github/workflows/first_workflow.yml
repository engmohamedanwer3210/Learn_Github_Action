name: First workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: say-hello
        run: echo "hello"
      - name: say-bye
        run: echo "goodbye!"
